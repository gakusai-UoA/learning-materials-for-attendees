// Section: 3. データ型 — プリミティブ型
// string（文字列）
const greeting = "こんにちは";

// number（数値）— 整数も小数も同じ型
const age = 21;
const pi = 3.14;

// boolean（真偽値）
const isStudent = true;

// null — 意図的に「値がない」ことを示す
const data = null;

// undefined — 値が未設定の状態
let x;
console.log(x); // undefined
