// Section: 5. 論理演算子 — AND / OR / NOT
// AND（かつ）— すべて true なら true
const canBuy = hasStock && hasMoney;

// OR（または）— どちらか true なら true
const canEnter = isMember || hasTicket;

// NOT（否定）— 反転
const isNotReady = !isReady;
