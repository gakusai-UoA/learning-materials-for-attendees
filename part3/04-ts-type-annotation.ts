// From: TypeScript の基本的な書き方：型注釈 (Type Annotation)
// 変数に型をつける
const userName: string = "Shake";
const price: number = 1000;

// 関数の引数と戻り値に型をつける
function greet(name: string): string {
  return `Hello, ${name}`;
}
