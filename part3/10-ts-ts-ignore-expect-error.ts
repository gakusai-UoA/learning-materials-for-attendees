// From: エラーを強制的に黙らせるコメント
// 例：外部ライブラリの型定義が古く、実際には存在する「os」プロパティがエラーになる場合

// @ts-ignore
// とにかくエラーを消す（もしライブラリが更新されても気づけない）
console.log(config.os);

// @ts-expect-error
// 「エラーが出るはず」と宣言（ライブラリが更新されて型が正しくなれば、ここがエラーになり気づける）
console.log(config.os);
